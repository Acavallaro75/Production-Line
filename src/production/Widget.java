package production;

/**
 * The Widget class is a temporary stub class to test the functionality of the project. It extends
 * the parent class, Product, and takes the following parameters: name, manufacturer, and itemType.
 * All the arguments are passed to the parent, Product, class as arguments.
 */
class Widget extends Product {
  Widget(String name, String manufacturer, ItemType itemType) {
    super(name, manufacturer, itemType);
  }
}
